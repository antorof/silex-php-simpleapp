<IfModule mod_rewrite.c>
    Options -MultiViews

    # DocumentRoot "/cc/web"
    
    RewriteEngine On
    # RewriteBase /cc/web
    
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [QSA,L]
</IfModule>